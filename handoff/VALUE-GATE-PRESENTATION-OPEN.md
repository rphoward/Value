(def-sop value-gate-presentation-open
  (context
    (target "fresh-session-agent")
    (optimization "multiple-viable-gate-presentation-options-before-ship")
    (outcome OPEN)
    (opened 2026-07-19)
    (session-slug value-design)
    (session-root workproduct/value-proposition/value-design/))

  <central_idea>
  (center-of-gravity
    (invariant "Value-map gate content is a usable hypothesis; gate comprehension failed. Fresh work owns hunting several presentation options via poteto arena and pstack tools, then picking one with evidence — not shipping the first mock or another monolithic dump."))
  </central_idea>

  (protocol-0-problem
    (failure "Dense text wall at gate review violated cognitive_murder; single Mermaid diagram could not show whole map — user: valueless information")
    (recorded-in session.json V08 reopen decision + evidence fact + blocking unknown on presentation format)
    (content-still-valid profile passed; value-map atoms V01–V07 accepted; weak indirect fit links labeled honestly; no orphans parked))

  (protocol-1-must-hunt-multiple-options
    (minimum-options 3)
    (forbidden 'single-option-rush 'first-mock-as-winner 'monolithic-gate-dump 'full-canvas-before-progressive-disclosure)
    (seed-candidates-not-exhaustive
      progressive-three-beat "segment → box → one link at a time"
      split-stickies "profile | offering | fit matrix cells = weak/conditional/direct only"
      ad-lib-first "Steve Blank pitch before any diagram (V08 kb ad-lib-formula)"
      gate-lens-export "new references/export-lenses.md section + template artifact"
      canvas-spatial "Cursor canvas for pannable map — chat stays one beat"
      scripted-strip-plus-detail "status --sections always; drill-down on ask only"))

  (protocol-2-poteto-and-arena
    (mode "/poteto-mode Feature — exhaust design space before production edits")
    (arena
      (path tools/drafts/value-gate-arena/)
      (deliverables "2–4 throwaway mocks or static HTML/MD samples — each a distinct presentation strategy")
      (pick-record handoff/decision-trails/value-gate-presentation.tsv or section in VALUE-GATE-PRESENTATION-PASSED.md)
      (delegate poteto-agent for exploration when parallel perspectives help))
    (architect "arena replaces full architect sketch when mocks exist; re-arena if production fill-in repeats friction")
    (principles "Exhaust the Design Space · Laziness · Boundary Discipline — presentation layer only; do not rewrite map atoms"))

  (protocol-3-pstack-and-other-tools
    (pstack-how "before touching skill package — how skill on progressive disclosure patterns")
    (pstack-arena-runners "composer-2.5, cursor-grok-4.5-high, gpt-5.6-sol-high per .cursor/rules/pstack-models.mdc")
    (pstack-cross-judge "cursor-grok-4.5-high, gpt-5.6-sol-high, composer-2.5 — discriminate or pair-judge arena finalists")
    (optional
      canvas-skill "if spatial mock is a finalist"
      quality-judge "score mocks against cognitive_murder + user failure criteria"
      drift-audit "if skill wording changes risk reintroducing text-wall gate behavior"))

  (protocol-4-read-first
    (session workproduct/value-proposition/value-design/session.json)
    (build-pack north-star-blurb.md value-trail.md CONTEXT.product.md)
    (skill .cursor/skills/value/SKILL.md references/export-lenses.md references/value-map.md references/session-contract.md)
    (failure-evidence session.json decisions reopen value-map gate + unknown gate presentation format)
    (paste-prompt handoff/NEW-CHAT-PROMPT-VALUE-GATE-PRESENTATION.md))

  (protocol-5-ship-criteria
    (pass-when
      "arena pick recorded"
      "at least 3 options compared with explicit tradeoffs"
      "chosen approach demonstrated on value-design session (dogfood)"
      "skill or lens diff is surgical — gate trigger only; not every turn"
      "tests updated if package contract changes"
      "handoff/VALUE-GATE-PRESENTATION-PASSED.md with evidence command output")
    (then "user may pass V08 on value-design session or resume business-model"))

  (protocol-6-out-of-scope-this-gate
    (defer "proving weak gain links — experiments module"
      "full book-figure reproduction"
      "Python engine under eliotapp unless user explicitly expands scope")))
