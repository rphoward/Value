# CLAUDE.md

Cursor loads [`.cursor/rules/`](.cursor/rules/) and this repo’s agent map automatically. This file exists only for Claude Code (and similar tools) that read `CLAUDE.md` at the project root—do not treat it as a second rule set.

@AGENTS.md

When a rule is not covered in AGENTS.md, open the matching `.mdc` under `.cursor/rules/` before editing.
