(def-ref workflow
  (linked-from protocol-1)
  (section purpose
    (note "Fill in the Value skill workflow steps here."))
  (section steps
    (check "Define trigger phrases in SKILL.md description")
    (check "Write numbered protocols in SKILL.md")
    (check "Move deep examples and rubrics into this references/ tree"))
  (forbidden 'unstructured-prose-blobs-without-checks))
