# Compile notes

Scaffolded by `scripted-skill-from-doc` from `docs/business-model-generation-prompt-suite.md`.

Curriculum expanded (not stub):
- canvas-mapper: empathy → nine blocks → left/right audit → gate
- pattern-innovator: epicenter → optional What-If → pattern choose/apply → gate
- strategy-evaluator: SWOT → Blue Ocean ER/RC → soft environment → gate
- ambidextrous-execution-designer: structure → phase → soft Galbraith → gate

Teach aid: `assets/knowledge-base.json` → `teach_aids.coffee_capsule_example`.

Peers: `value`, `lean-mvp`, `product-spine`. Do not promote over `value`.

Promoted 2026-07-29 to `.cursor/skills/bmg/` and `skills/bmg/` (with `/product-spine` re-entry on SKILL.md). Re-promote only with `--force --overwrite-slug bmg` after curriculum edits.
