# prompt-suite-compile (shim)

The full pack lives at `.cursor/skills/scripted-skill-from-doc/` (`scripts/`, `references/`, `assets/`).

Scripts here forward to that skill’s `scripts/`. Prefer:

```text
python .cursor/skills/scripted-skill-from-doc/scripts/compile.py check
```

