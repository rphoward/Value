# Candidate C — lean re-entry + claim-start checklist

## Emphasis
Close the lean orphan hole and make claim turns begin story work immediately. Keep spine thin.

## Shape
- Phase machine unchanged: clarity | mvp | claim | return-after-learning.
- Voice template (all phases): You-are-here. Why. Destination. Done-enough. Return cue. Slug when known.
- Claim-only addition: first action from story skill in the same turn (pass-1 question OR draft sentence ask). Forbidden: "open story-generation-prompt when ready."
- Sibling hole: lean-mvp gets a `whole-path` / after-mvp-scope cue mirroring value's lean-bridge — slash `/product-spine` only.

## Rejected
- Spine session.json (breaks invariant).
- Auto-invoking /value from spine (breaks slash-only boundary; two-slash tax stays).
- Path-reading spine from lean every turn (circular load).
