# Usage mocks — candidate C (Maya / ShiftSwap)

## 1. Clarity (no session)

You are at the start of ShiftSwap: a vibecode idea that is not yet valuable on paper.

Next step is clarity. Invoke `/value` and we will pin who this is for and the value map for slug `shiftswap`.

Done enough when profile and value-map gates are passed or bypassed. Then invoke `/product-spine` again.

Why this phase: there is no value session yet, so MVP and pitch come after clarity.

## 2. MVP (clarity-ready)

You are past clarity for `shiftswap`. Profile and value map are done enough.

Next step is the MVP wedge. Invoke `/lean-mvp` for the same slug.

Done enough when the mvp-scope gate is passed or bypassed. Then invoke `/product-spine` again so we can claim.

Why this phase: clarity is ready and lean is not finished, so features come before the pitch.

## 3. Claim (mvp-ready)

You are ready to claim `shiftswap`: an honest shareable story, not more canvas grilling.

I am starting the claim path now. We will land one INVEST sentence and, if you want video, a NotebookLM producer paste.

Before the sentence, answer this NotebookLM pass-1 style question in your own words: what does ShiftSwap do in one breath for a restaurant server trading a shift, and what must stay true for that to be honest?

Done enough when you have the story sentence and, if you want overview video, the producer paste block. Invoke `/product-spine` if you get lost after that.

Why this phase: clarity and mvp-scope are done enough, so the exit is claim, not another skill door.
