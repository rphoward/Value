# Skill delta — Candidate B (minimal)

Ship under `.cursor/skills/product-spine/` (+ digest-match `skills/product-spine/`). Sibling edit only for proven lean orphan. No spine `session.json`.

## product-spine / `SKILL.md`

- **protocol-2-voice** — mandate fixed guide-turn order: you-are-here → one destination → done-enough → return cue (`/product-spine` again when lost or leg complete). Forbid triage-only one-liners that name a slash and stop without done-enough + return cue.
- **protocol-1 readiness** — state explicitly: `clarity-ready` / `mvp-ready` come only from sibling `module_outcome` ∈ `{completed, bypassed}` (plus explicit lean-skip for claim). Status brief = position hint only; never treat active module as readiness.
- **protocol-0** — keep claim: if phase is claim, read and follow `story-generation-prompt` in this turn (no extra claim choreography protocol).
- **central_idea / description** — one line that spine is re-entrance until claim work is in hand; lean/value may name `/product-spine` on exit without loading spine every turn.

## product-spine / `references/path.md`

- **section voice** — mirror the four-beat contract; add check that readiness is not inferred from status active module.
- **section reading-sibling-state** — one line: outcomes drive ready flags; status drives “where you are inside the sibling,” not phase exit.
- **maya-happy-path** — after step-2, note lean’s bounce-back line returns her to `/product-spine` for claim.

## lean-mvp / `SKILL.md` (optional but recommended — gap #1)

- On mvp-scope done-enough (completed|bypassed), or when human asks “what next / I’m lost,” close with one sentence: return to **/product-spine** for claim (or return-after-learning). Do **not** load `product-spine/SKILL.md` every lean turn.
- Do not add spine phase logic inside lean — only the re-entry cue.

## value / `SKILL.md` (defer unless mock proves orphan)

- Same one-line bounce-back after profile+value-map done-enough — only if clarity→mvp handoff fails the same way lean→claim does.

## Out of scope

- No spine scripts/, no spine session, no coordinator agent, no auto-accept/init from spine, no rewriting story-generation-prompt unless claim mock shows a dump hole.
