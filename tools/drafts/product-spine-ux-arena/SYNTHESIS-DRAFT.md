# Synthesized design (parent draft — pending judge)

## Base lean (Candidate B)
Four-beat guide-turn: you-are-here → destination → done-enough → return cue.
Readiness only from module_outcome completed|bypassed.
Lean bounce-back to `/product-spine` after mvp-scope done-enough.
Claim = follow story-generation-prompt this turn; no extra story orchestra in spine.

## Graft from A
- Explicit illegal-replies list (routing-only claim; readiness from status brief; stop without done-enough+return).
- Claim must execute story skill before turn ends (not name-only).

## Graft from C
- Claim opening must take a first story action in-turn (pass-1 style question or draft ask), not only announce following the skill.

## Reject
- Spine session.json / fifth coordinator (all candidates agree).
- Auto-invoke /value (breaks slash boundary).
- Heavy multi-beat story machine inside spine (B correctly rejected A's "stay until paste in hand" as over-coupled; story skill owns pacing after first action).
- Value bounce-back unless Maya walk proves orphan (value already has whole-path + after-value-map lean offer).
