# Product-spine walk findings

Generated by `python tools/drafts/product-spine-walk/walk_spine.py`.

## What ran

Six mock paths against a temp `workproduct/` tree: bare idea, repo claim, value-only resume, dual sessions, MS05 story paste, MVP ask with no session. Package tests: `test_product_spine_skill`, `test_lean_mvp_coaching`, `test_lean_mvp_skill_package` (18 passed after coaching fix).

## Fixed during the walk

`story_assist` lived in `atom-coaching.json` but `coaching_for_atom` dropped unknown fields. MS05 turns never saw the offer. Forwarded in `.cursor/skills/lean-mvp/scripts/_session/coaching.py` (mirrored under `skills/`).

## Highest human UX bottlenecks left

1. **Two slash commands before work.** `/product-spine` only names the destination; human must open `/value` or `/lean-mvp` next. First product question is still several turns away.
2. **NotebookLM leaves Cursor.** Repo-claim path has no durable session. Resume after pass-1 is memory, not triage.
3. **Spine is blind in chat.** Dual-session precedence is correct in prose, but the human never sees why they were routed. Spine forbids running `status.py`.
4. **MS05 still wants a manual paste.** Seven steps across lean ↔ story. Honest by design; painful in practice.
5. **"Customer clarity enough" is self-report.** Spine can send someone to lean-mvp with an empty value tree; C01 restarts segment grilling.

## Secondary

- Missing-session name + consent after triage.
- Silent slug derivation; hard to resume by memory.
- Value gate pass needs a decisions records sidecar (agents forget; milestones fail).
- `import_value_context` resolved against repo `workproduct/`, not the temp lean root sibling, during the walk. Fine for normal monorepo layout; brittle if cwd or roots diverge.

## Artifact

`tools/drafts/product-spine-walk/last-walk.json`
