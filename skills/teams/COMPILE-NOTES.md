# Compile notes

Scaffolded by `prompt-suite-compile` from `docs/High-Impact Tools Suite.md`.

- Curriculum expanded (real asks; no stub placeholders). Audit/smoke ok.
- Bounce: after tam-planner gate → invoke `/product-spine` (canvas-mapper parallel).
- Promoted 2026-07-31 to `.cursor/skills/teams/` and `skills/teams/`.
- Peers: `value`, `bmg`, `lean-mvp`, `product-spine`. Do not promote over `value`.
